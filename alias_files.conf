# Format: status|datei|kurzbeschreibung
# status: enabled oder disabled
# Alternativ kann eine Zeile komplett auskommentiert werden.

enabled|00-core.sh|Basis-Aliase fuer alle Benutzer
enabled|10-navigation.sh|Navigation und sichere Defaults
enabled|20-files.sh|Datei- und Speicher-Helfer
enabled|30-process.sh|Prozess-Helfer
enabled|40-network.sh|Netzwerk-Helfer
enabled|50-root-journal.sh|Journald-Funktionen (nur root)
enabled|60-root-apt.sh|APT-Shortcuts und Update-Workflow (nur root)
enabled|70-root-systemd.sh|Systemd-Shortcuts mit Tab-Completion (nur root)
enabled|90-overrides.sh|Lokale server-spezifische Overrides
enabled|99-help.sh|Hilfsfunktion zur Alias-Anzeige
