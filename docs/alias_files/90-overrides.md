# 90-overrides.sh

Lokale Overrides und Reload-Helfer.

## Enthalten
- `rload`: `~/.bashrc` neu laden
- `e_alias`: `~/.bash_aliases` bearbeiten und neu laden
- Optionales Laden von:
  - `/opt/scripts/bash_aliases_specific`
  - `~/.bash_aliases_specific`
